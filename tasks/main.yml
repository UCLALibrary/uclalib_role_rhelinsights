---

- name: Ensure Red Hat Insights Client in installed
  yum:
    name: "{{ insights_packagename }}"
    state: present

- name: Register to the Red Hat Insights Service
  command: >
    {{ insights_clientname }} --register
  args:
    creates: "{{ insights_client_conf_dir }}/.registered"
